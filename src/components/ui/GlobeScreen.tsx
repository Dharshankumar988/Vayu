"use client";
// GlobeScreen.tsx — referenced from page.tsx but not actively used
// The globe is now embedded in RegionalView (admin) and HostServers (client).
// This stub prevents the dynamic import in page.tsx from failing.
export default function GlobeScreen() {
  return null;
}
